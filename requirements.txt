# ── Server ────────────────────────────────────────────────────────────────────
websockets>=12.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
aiohttp>=3.9.0

# ── LLM / API ─────────────────────────────────────────────────────────────────
openai>=1.30.0                  # OpenRouter uses OpenAI-compatible client
httpx>=0.27.0

# ── Storage ───────────────────────────────────────────────────────────────────
# SQLite is stdlib — no package needed
# ChromaDB removed — replaced by FTS5 + embeddings in SQLite

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0
torch>=2.2.0                    # required by sentence-transformers
numpy>=1.26.0

# ── PDF generation ────────────────────────────────────────────────────────────
reportlab>=4.1.0

# ── Audio / transcription (Pi side) ──────────────────────────────────────────
# faster-whisper>=1.0.0         # install separately on Pi — heavy
# pyaudio>=0.2.14               # install separately on Pi

# ── TTS ───────────────────────────────────────────────────────────────────────
elevenlabs>=1.3.0

# ── SMS / voice ───────────────────────────────────────────────────────────────
twilio>=9.0.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pytz>=2024.1
tzlocal>=5.2
aiofiles>=23.2.1